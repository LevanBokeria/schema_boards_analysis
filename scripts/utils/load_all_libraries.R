# Description ##################################

# This script will load the libraries that I commonly use, so I don't have to
# put a huge statement at the start of each code

# Load ###########################################

pacman::p_load(pacman,
               rio,
               rstatix,
               DT,
               kableExtra,
               readr,
               writexl,
               jsonlite,
               stringr,
               gridExtra,
               knitr,
               magrittr,
               Hmisc,
               psycho,
               data.table,
               pdist,
               moments,
               LambertW,
               tidyverse,
               BayesFactor,
               ggpubr,
               forestmangr,
               broom,
               nlme)
